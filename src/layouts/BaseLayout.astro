---
// BaseLayout.astro — Layout base del sitio. Incluye tokens, reset, iconos, topbar,
// header, prefooter, footer y el script de inicialización de UI global.
import '../styles/tokens.css';
import '../styles/fonts.css';
import '../styles/base.css';
import Icons from '../components/Icons.astro';
import Topbar from '../components/Topbar.astro';
import Header from '../components/Header.astro';
import PrefooterCta from '../components/PrefooterCta.astro';
import Footer from '../components/Footer.astro';
import WhatsappFab from '../components/WhatsappFab.astro';
import Toast from '../components/Toast.astro';
import QuoteDrawer from '../components/QuoteDrawer.astro';
import QuickView from '../components/QuickView.astro';
import { ClientRouter } from 'astro:transitions';
import { organizationGraph, type JsonLdNode } from '../lib/seo';
import { asset } from '../lib/asset';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  hidePrefooter?: boolean;   // oculta el CTA pre-footer global (ej. Kits usa su propio banner)
  schema?: JsonLdNode | JsonLdNode[];   // nodos JSON-LD propios de la página (Product, FAQPage, etc.)
  preloadImage?: string;   // ruta /public de la imagen LCP above-the-fold (ej. fondo del hero) → preload
}

const {
  title,
  description = 'Ducha Segura® — Rebajes de tina para acceso seguro. Producto nacional patentado. Garantía 3 años.',
  ogImage = '/images/og-default.png',
  hidePrefooter = false,
  schema,
  preloadImage,
} = Astro.props;

// El fondo del hero es un background-image (se descubre tarde) → preload explícito para el LCP.
const preloadImageUrl = preloadImage ? asset(preloadImage) : null;

const canonical = new URL(Astro.url.pathname, Astro.site);
const ogImageUrl = new URL(ogImage, Astro.site);

// Un único bloque @graph: Organization/WebSite global + nodos de la página.
const pageNodes = schema ? (Array.isArray(schema) ? schema : [schema]) : [];
const jsonLd = {
  '@context': 'https://schema.org',
  '@graph': [...organizationGraph(Astro.site!), ...pageNodes],
};
---
<!doctype html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <!-- Progressive enhancement: marca `js` para gatear la animación .reveal.
       Sin JS, el contenido .reveal se muestra de inmediato (no queda invisible). -->
  <script is:inline>document.documentElement.classList.add('js')</script>
  <!-- Google Tag Manager -->
  <script is:inline>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
  new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
  j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
  'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
  })(window,document,'script','dataLayer','GTM-WRGMCKWC');</script>
  <!-- End Google Tag Manager -->
  <meta name="description" content={description} />
  <meta name="theme-color" content="#0072C0" />
  <title>{title}</title>
  <link rel="canonical" href={canonical} />
  <link rel="icon" type="image/png" href="/images/favicon.png" />
  <link rel="apple-touch-icon" href="/images/Ducha segura logo azul.png" />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:image" content={ogImageUrl} />
  <meta property="og:type" content="website" />
  <meta property="og:url" content={canonical} />
  <meta property="og:site_name" content="Ducha Segura®" />
  <meta property="og:locale" content="es_CL" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={title} />
  <meta name="twitter:description" content={description} />
  <meta name="twitter:image" content={ogImageUrl} />
  <script type="application/ld+json" is:inline set:html={JSON.stringify(jsonLd)} />
  <!-- Fuentes auto-hospedadas (src/styles/fonts.css). Elimina el dominio externo de la ruta
       crítica; preload solo de las 2 latinas above-the-fold para minimizar swap (LCP + CLS). -->
  <link rel="preload" href="/fonts/inter-400-latin.woff2" as="font" type="font/woff2" crossorigin />
  <link rel="preload" href="/fonts/poppins-800-latin.woff2" as="font" type="font/woff2" crossorigin />
  {preloadImageUrl && <link rel="preload" href={preloadImageUrl} as="image" fetchpriority="high" />}
  <ClientRouter />
</head>
<body>
  <!-- Google Tag Manager (noscript) -->
  <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-WRGMCKWC"
  height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
  <!-- End Google Tag Manager (noscript) -->
  <a href="#main" class="skip-link">Saltar al contenido principal</a>
  <Icons />
  <Topbar />
  <Header />
  <main id="main">
    <slot />
  </main>
  {!hidePrefooter && <PrefooterCta />}
  <Footer />
  <WhatsappFab />
  <Toast />
  <QuoteDrawer />
  <QuickView />
  <script>
    import { initUI } from '../scripts/ui';
    import { initCart } from '../scripts/cart';
    import { initQuickView } from '../scripts/quickview';
    import { initSearch } from '../scripts/search';
    import { initCotizar } from '../scripts/cotizar';
    document.addEventListener('astro:page-load', () => { initUI(); initCart(); initQuickView(); initSearch(); initCotizar(); });
  </script>
</body>
</html>
