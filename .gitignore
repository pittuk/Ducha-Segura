# build output
dist/
.output/
.vercel/
.netlify/

# dependencies
node_modules/

# Astro
.astro/

# env / secrets (claves de pasarela de pago a futuro)
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# Backend PHP — credenciales reales (no commitear)
public/api/config.php

# Credenciales de despliegue (SSH/FTP Hostinger) — NUNCA commitear
CREDENCIALES*.md
CREDENCIALES*.txt
*.credentials
deploy-secrets*

# Paquete de despliegue (pesado)
*.zip

# Material fuente de prensa (pesado: Word/PDF/videos) — no va al repo
docs/PRENSA/
